INT32 i;
INT32 a[10];
INT32 b[10];
INT32 c[10];

FOR (i: (0, 10))
{
    a[i] = i;
    b[i] = i + 1;
};

FOR (i: (0, 10))
{
    INT32 v;
    INT32 w;

    v = 0;
    w = 0;

    IF (i < 9)
    {
        v = a[i + 1];
    };

    IF (i > 0)
    {
        w = a[i - 1];
    };

    c[i] = v * w;

    PRINT "c[", i, "] = ", c[i], " ( = ", v, " * ", w, " )";
};
